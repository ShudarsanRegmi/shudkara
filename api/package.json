{
  "name": "api",
  "version": "1.0.0",
  "description": "Azure Functions API for Shudkara Text Rooms",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "mongodb": "^6.13.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.19",
    "rimraf": "^5.0.5",
    "typescript": "^5.7.3"
  }
}
